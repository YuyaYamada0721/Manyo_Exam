<h1>タスク一覧画面</h1>
  <table>
    <tr>
      <th>タスク</th>
      <th>内容</th>
    </tr>
  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.task_name %></td>
      <td><%= task.task_content %></td>
      <td><%= link_to '詳細', task_path(task.id) %></td>
    </tr>
  <% end %>
  </table>
