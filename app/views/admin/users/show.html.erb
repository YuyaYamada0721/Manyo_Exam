<%= link_to '管理画面', admin_users_path, class: "btn btn-default"%>
<%= link_to 'タスク管理へ', tasks_path, class: "btn btn-default"%>

<div class='profile'>
  <p><%= @user.username %>のページ</p>
  <p>メールアドレス: <%= @user.email %></p>
</div>

<p class='admin-label'>終了間近のタスク</p>
  <table class="table">
    <tr>
      <th>タスク</th>
      <th>内容</th>
      <th>終了期限</th>
      <th>ステータス</th>
      <th>優先順位</th>
    </tr>
    <% @near_the_end_task.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.content %></td>
        <td><%= task.expiration_deadline %></td>
        <td><%= task.status %></td>
        <td><%= task.priority %></td>
      </tr>
    <% end %>
  </table>

<p class='admin-label'>期限切れのタスク</p>
  <table class="table">
    <tr>
      <th>タスク</th>
      <th>内容</th>
      <th>終了期限</th>
      <th>ステータス</th>
      <th>優先順位</th>
    </tr>
    <% @expired_task.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.content %></td>
        <td><%= task.expiration_deadline %></td>
        <td><%= task.status %></td>
        <td><%= task.priority %></td>
      </tr>
    <% end %>
  </table>
